// examples/demo.ltl — runnable demo for ltl_scheduler_demo

import std::io
use ltl_scheduler_demo as lib

fn main() {
    io::println("ltl_scheduler_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}